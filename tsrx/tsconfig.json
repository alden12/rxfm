{
  // Governs .tsrx files in the editor. The root tsconfig deliberately excludes
  // `tsrx/`, which would otherwise leave .tsrx files in an inferred project with
  // default options — no `paths`, unreliable extraFileExtensions wiring, so the
  // tsrx tsserver plugin's virtual TypeScript never type-checked properly (hovers
  // showed `any`). This pins the same options the transform itself uses
  // (see ts-plugin/transform.cjs `getCompilerOptions`) plus the `rxfm` path.
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "baseUrl": ".",
    "paths": {
      "rxfm": ["../src/lib/rxfm/index"]
    }
  },
  "include": ["**/*.tsrx", "**/*.ts"],
  "exclude": ["node_modules", "dist-tsrx", "ts-plugin"]
}
