import { Div } from "rxfm";
import { timer } from "rxjs";

const tick = timer(0, 1000);
const visible = tick % 2 === 0;

export const ConditionalComponentsExample = Div(
  visible ? Div`Now you see me!` : null,
);
